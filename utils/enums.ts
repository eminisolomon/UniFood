export enum RoleType {
  USER = 'user',
  ADMIN = 'admin',
  SUPPORT = 'support',
}

export enum AccountStatus {
  Active = 'activate',
  DEACTIVE = 'deactivate',
}

export enum OtpType {
  FORGET = 'forget',
  VERIFICATION = 'verification',
  FORGET_PIN = 'forget_pin',
}
